# Attribution

The runtime imports the adjacent SeaCache4Wan21 integration, derived from the
Alibaba Wan Team's Wan2.1 (Apache-2.0) and the documented SeaCache integration.
See `../SeaCache4Wan21/NOTICE.md` and `upstream_lock.json` for attribution and
source provenance. This project replaces cache scheduling with an explicit
single-step intervention; it does not claim to reproduce threshold SeaCache.

The raw G1 pooling layout follows the local formal Ours4Wan21 implementation
`experiments/cnn_mixed3500_v1/features.py`; no trained model weights are included.
