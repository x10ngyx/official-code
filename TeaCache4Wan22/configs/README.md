# Configs

`wan22_t2v_a14b_50step_dpmpp.json` records the only inference protocol
supported by TeaCache4Wan22 v0.1. Runtime coefficient validation uses the same
fields and fails closed on mismatches.
