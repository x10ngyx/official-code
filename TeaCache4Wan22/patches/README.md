# Patches

`wan22_42bf4cf_teacache.patch` contains only the integration changes to the
locked upstream Wan2.2 source. The TeaCache algorithm itself remains in the
directly reviewable `runtime/teacache.py` file and is copied into the prepared
tree by `scripts/prepare_wan22.sh`.
