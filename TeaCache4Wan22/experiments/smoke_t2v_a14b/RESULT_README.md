# TeaCache4Wan22 fixed-protocol smoke result

This external result directory contains a matched no-cache/TeaCache generation
pair, structured inference-only timing, complete TeaCache decisions, artifact
manifests, component latency/TFLOPs, canonical RGB PSNR/SSIM/LPIPS, custom
VBench baseline/candidate scores, and `summary.json`. The source launcher is
`experiments/smoke_t2v_a14b/run_pair.sh`.
