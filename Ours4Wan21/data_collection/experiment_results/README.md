# Experiment results

Every generated archive must be stored below the external result base
(`EXP_BASE`, default `/all/yiran07-disk3/huteng_data/exp`). Add only a symlink
here after an actual run is created.
