# rl_analysis_v1

run.sh invokes analyze_training.py in conda wan2.2 with explicit BLAS thread limits. See ../../README.md for the remote workflow and all required arguments. Results must be under the configured external result root; models remain under the declared workspace models/.
