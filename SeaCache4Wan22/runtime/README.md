# Runtime

- `seacache.py`: canonical stage-aware controller, SEA filter, shared CFG gate,
  branch-separated residuals, boundary handling, trace, and protocol checks.
- `inference_timing.py`: CUDA-synchronized `WanT2V.generate()` timing.

This directory intentionally contains no other cache implementation.
