# Patches

`wan22_42bf4cf_seacache.patch` is generated against the exact Wan2.2 commit in
`upstream_lock.json`. It modifies only `generate.py`, `wan/text2video.py`, and
`wan/modules/model.py`; the canonical controller is installed separately from
`runtime/seacache.py`.
