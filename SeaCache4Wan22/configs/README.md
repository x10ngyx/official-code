# Configs

`wan22_t2v_a14b_50step_dpmpp.json` records the one fixed model-side protocol
accepted by the public runner. SeaCache threshold and retention mode remain
method parameters and must be recorded per run.
