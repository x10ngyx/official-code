# Fixed protocol launcher

Run `bash run.sh <main.py arguments>` with conda wan2.2 active, or set WAN22_PYTHON to that environment. The launcher sets all four thread limits. Outputs must be on the external experiment disk. See ../../README.md for full commands.
