# Experiments

`reinforce_v1/` contains the complete versioned CNN+G1 on-policy REINFORCE ablation.
All generated artifacts belong on the external exp disk, linked from the project experiment_results directory.

- `training_diagnostics_v1/`: read-only batch PSNR, exact-K comparisons, same-prompt/K matching and entropy plots; does not change sealed training code.
